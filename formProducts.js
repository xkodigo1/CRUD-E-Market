class formProduct {
    constructor(codigo, nombre, stock, precio, imagen) {
        this.codigo = codigo
        this.nombre = nombre
        this.stock = stock
        this.precio = precio
        this.imagen = imagen
    }
}